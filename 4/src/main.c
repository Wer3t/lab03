// Дано 4-розрядне число у системі числення р. Визначити його еквівалент у десятковій системі числення.
int main() {
	int  p=8, ch=4567;
	int ch10 = (((ch-ch%1000)/1000)*p*p*p)+(((ch%1000-((ch%1000)%100)/100))*p*p)+((((ch%1000)%100)-(((ch%1000)%100)%10)/10)*p)+(((ch%1000)%100)%10);
	return 0;
}
